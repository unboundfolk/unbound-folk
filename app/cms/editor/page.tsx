import { AdminPanel } from "@/components/cms/admin-panel";

export const metadata = { title: "CMS Editor — Unbound Folk", robots: { index: false } };

export default function EditorPage() {
  return <AdminPanel />;
}
